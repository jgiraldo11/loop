function arrayCounter(array) {
    for (let i=0; i<array.length; i++) {
        const result = array [i]
        console.log(result);
    }
}

const myShirt = ["cotton","silk","poly","linen"]

arrayCounter(myShirt);